# grade-checker
A simple Python program that converts scores into grades.


## What it does

This program takes a student's score as input and assigns a grade from A to F.

It also allows multiple scores to be entered until the user chooses to quit.

## How to use

Run the Python file and enter your score when prompted.

Type `q` to stop the program.
